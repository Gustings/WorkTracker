<Window x:Class="WorkTracker.Views.ClosePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Close Work Tracker" 
        Height="220" Width="400"
        Background="Transparent"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        WindowStartupLocation="CenterOwner">
    
    <Border BorderThickness="2" BorderBrush="{StaticResource BorderBrush}" CornerRadius="12" Background="{StaticResource BgBrush}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                <Ellipse Width="12" Height="12" Fill="{StaticResource PrimaryAccentBrush}" Margin="0,0,10,0" />
                <TextBlock Text="Close Work Tracker?" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextMainBrush}" />
            </StackPanel>

            <!-- Message -->
            <TextBlock Grid.Row="1" TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource TextSecBrush}" LineHeight="18" Margin="0,0,0,15">
                Would you like to minimize to the system tray to continue tracking your work in the background, or exit the application completely?
            </TextBlock>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Minimize to Tray" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0" Padding="12,6" Click="Minimize_Click"/>
                <Button Content="Exit Application" Style="{StaticResource DangerButtonStyle}" Margin="0,0,8,0" Padding="12,6" Click="Exit_Click"/>
                <Button Content="Cancel" Style="{StaticResource ModernButtonStyle}" Padding="12,6" Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
